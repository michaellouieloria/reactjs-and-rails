<% title("Contact") %>
<h1 class="page-title">Contact</h1>

<%= react_component('ContactForm',
                    {
                      :form => {
                        :action => email_confirmation_path,
                        :csrf_param => request_forgery_protection_token,
                        :csrf_token => form_authenticity_token
                      }
                    },
                    { prerender: true }) %>
